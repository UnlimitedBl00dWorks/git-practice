git init = initialized the repo
git status = tells you about changes (i.e. the status) since the last commit

#staging the commit
git add [filename] [filename] ... #files
git add . #all the changes

#make the committ
git commit -m "a description of the changes"

